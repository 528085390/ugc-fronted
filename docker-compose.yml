version: '3.8'

networks:
  bbs-net:
    external: true

services:
  frontend:
    image: nginx:alpine
    container_name: bbs-frontend
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./dist:/usr/share/nginx/html:ro
    networks:
      - bbs-net
